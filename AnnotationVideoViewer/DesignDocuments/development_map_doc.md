Ok This will serve as a brainstorming document for me while I code so I can come back and remember things I need to implement.

Goal:
An app that lets you open and annotate a folder of images.
You should be able to import an image, folder of images, or maybe even video and create a new project
you should be able to import an annotated folder from the other tool we are using and convert that data into this format

You should be able to save a project you are working on
You should be able to possibly export the mask data back out to that other tools format.

An AI algorithm will need to be able to add, edit, and delete annotations.

Annotations should include bounding boxes and a mask

Annotations should be renderable on different layers
Annotations should be able ot have different colors.

When creating a new project:
A pop up should appear where the user can define the project name and maybe ID.
You should probably also define the save location?

When saving a project:
It should auto save to the designated location

When saving as:
It should open a popup so you can define what its saved as and where

Exporting:
Is this necissary?

When viewing:
You shoudl be able to see the image
be able to move between frames if applicable
be able to see different annotation layers
interact with annotations (view, edit, delete)